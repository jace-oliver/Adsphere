import "./globals.css";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Interactive Marketing Sphere",
  description: "Explore ads across a 3D world: Entertainment Nation, Product Peninsula, Clothing Continent."
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
